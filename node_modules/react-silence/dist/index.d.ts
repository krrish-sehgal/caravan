declare type LogType = "error" | "log" | "warn" | "info";
/**
 * Turns off the console for the given log types and any text matching the given matchers.
 * Will remain off until a matching `unsilence` call.
 * */
export declare const silence: (...logTypesOrMatchers: Array<LogType | RegExp>) => void;
/**
 * Restores normal console functionality. If given types, restores for that type.
 * By default, unsilences all console log types.
 * */
export declare const unsilence: (...logTypes: LogType[]) => void;
/** Turns silence on in a before block in the describe, and then unsilences after. */
export declare const silenceDescribe: (...logTypesOrMatchers: Array<LogType | RegExp>) => void;
export declare const silenceWithin: (action: () => void, ...logTypesOrMatchers: Array<LogType | RegExp>) => void;
/** Creates an returns set of silencer functions configured to the same set of log types and matchers. */
export declare const silenceFor: (...logTypesOrMatchers: Array<LogType | RegExp>) => {
    silence: () => void;
    unsilence: () => void;
    silenceDescribe: () => void;
    silenceWithin: (action: any) => void;
};
export declare const matchers: {
    reactKeysError: RegExp;
    jestActError: RegExp;
    deprecationWarning: RegExp;
};
/** Silences within the body of a React component. */
export declare const useSilence: (...logTypesOrMatchers: Array<LogType | RegExp>) => void;
export {};
